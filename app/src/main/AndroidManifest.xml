<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalmovil" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalMovil" >
        <activity
            android:name=".Menuprincipal"
            android:exported="false"
            android:label="@string/title_activity_menuprincipal"
            android:theme="@style/Theme.FinalMovil.NoActionBar" />
        <activity
            android:name=".Registrarse"
            android:exported="false"
            android:theme="@style/Theme.FinalMovilDos" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.FinalMovilDos" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>